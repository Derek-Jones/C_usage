
struct i * f (struct p * q, long
#line 99
r);

i+j;

"abc" + 'd';

while ((a == '_') ||
       b(c) ||
       d(e))
   {
   if (ident_len < IDENT)
      id_str[ident_len]=cur_ch;
   ident_len++;
   do_cur_ch();
   }

x+y*z;

struct key_tag {
                char *key_str;
                int tok_num,
                    occurrences;
               };

#endif

x*y+z; // as per that other language

{

#define foo bar

